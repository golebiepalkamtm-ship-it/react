receivers:
  otlp:
    protocols:
      grpc: {}
      http: {}

exporters:
  logging:
    verbosity: detailed
  otlp:
    endpoint: "http://localhost:4317"
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [logging]
